# tasktracker
A lightweight, dynamic command-line interface (CLI) application built in Go for managing daily tasks. Features custom JSON serialization/deserialization workflows, memory allocation optimization via pointers, and dynamic file I/O operations for robust data persistence.
